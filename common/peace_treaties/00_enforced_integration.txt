po_enforced_integration = {
	category = 6 
	power_projection = vassalized_rival 
	power_cost_base = 0.0 
	prestige_base = 0.1
	ae_base = 0
	warscore_cost = {
		all_provinces = 0.0 
		no_provinces = 65.0 
		owner_keeps = 0.0 
		conquered = 0.0 
		returned_core = 0.0 
		released_nation = 0.0 
		cancelled_subject = 0.0 
		concede_colonial_region = 0.0 
	}
	warscore_cap = 50
	#requires_demand_independence = yes 
	is_make_subject = no
	requires_is_allowed = yes
	
	is_visible = { 
		has_country_flag = assertive_diplomacy_flag
		OR = {
			FROM = {
				is_neighbor_of = ROOT
			}
			any_subject_country = {
				is_neighbor_of = FROM
			}
		}
	}
	is_allowed = { 
		always = yes
	}
	effect = {
		ROOT = {
			create_subject = {
				subject_type = integrated_territory
				who = FROM
			}
			FROM = {
				change_country_color = {
					country = ROOT
				}
			}
		}
	}
	ai_weight = { 
		export_to_variable = {
			variable_name = ai_value 
			value = 1
		}
	}
}

