po_fragment_realm = {
	category = 6 
	power_projection = vassalized_rival 
	power_cost_base = 0.0 
	prestige_base = 0.1
	ae_base = 0
	warscore_cost = {
		all_provinces = 0.0 
		no_provinces = 65.0 
		owner_keeps = 0.0 
		conquered = 0.0 
		returned_core = 0.0 
		released_nation = 0.0 
		cancelled_subject = 0.0 
		concede_colonial_region = 0.0 
	}
	warscore_cap = 50
	#requires_demand_independence = yes 
	is_make_subject = no
	requires_is_allowed = no
	
	is_visible = { 
		FROM = {
			is_part_of_hre = yes
			capital_scope = {
				OR = {
					region = north_german_region
					region = south_german_region
				}
			}
			culture_group = germanic
		}
	}
	is_allowed = { 
		custom_trigger_tooltip = {
			tooltip = has_not_centralized_realm_yet
			FROM = { NOT = { has_country_flag = centralized_realm } }
		}
	}
	effect = {
		FROM = {
			decentralize_realm = yes
		}
	}
	ai_weight = { 
		export_to_variable = {
			variable_name = ai_value 
			value = 100
		}
	}
}

