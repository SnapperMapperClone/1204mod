epirote_wars_2 = {
	slot = 2
	generic = no
	ai = yes
	potential = { tag = EPI }
	has_country_shield = yes

	EPI_albania = {
		icon = mission_non-western_cavalry_raid
		position = 2
		required_missions = { EPI_force_limit }
		provinces_to_highlight = {
			area = albania_area
			NOT = { country_or_non_sovereign_subject_holds = ROOT }
		}
		trigger = {
			albania_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		effect = {
			give_claims = { area = apulia_area }
			give_claims = { area = calabria_area }
			give_claims = { area = vardarska_area }
		}
	}
	EPI_magna_graecia = {
		icon = mission_non-western_cavalry_raid
		position = 3
		required_missions = { EPI_albania }
		provinces_to_highlight = {
			OR = {
				area = apulia_area
				area = calabria_area
			}
			NOT = { country_or_non_sovereign_subject_holds = ROOT }
		}
		trigger = {
			apulia_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
			calabria_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		effect = {
			give_claims = { area = sicily_area }
			give_claims = { area = western_sicily_area }
			give_claims = { area = naples_area }
		}
	}
	EPI_sikelia = {
		icon = mission_non-western_cavalry_raid
		position = 4
		required_missions = { EPI_magna_graecia }
		provinces_to_highlight = {
			OR = {
				area = sicily_area
				area = western_sicily_area
			}
			NOT = { country_or_non_sovereign_subject_holds = ROOT }
		}
		trigger = {
			sicily_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
			western_sicily_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		effect = {
			give_claims = { area = lazio_area }
			give_claims = { area = central_italy_area }
		}
	}
}

epirote_wars_3 = {
	slot = 3
	generic = no
	ai = yes
	potential = { tag = EPI }
	has_country_shield = yes

	EPI_force_limit = {
		icon = the_latin_empire
		position = 1
		required_missions = { }
		provinces_to_highlight = { }
		trigger = {
			army_size_percentage = 1.0
		}
		effect = {
			give_claims = { area = hellas_area }
			give_claims = { area = macedonia_area }
			add_core = 4175
			add_core = 4174
			add_core = 2750
			add_core = 1853
			
		}
	}
	EPI_hellas = {
		icon = mission_non-western_cavalry_raid
		position = 2
		required_missions = { EPI_force_limit }
		provinces_to_highlight = {
			area = hellas_area
			NOT = { country_or_non_sovereign_subject_holds = ROOT }
		}
		trigger = {
			hellas_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		effect = {
			give_claims = { area = morea_area }
			give_claims = { area = crete_area }
		}
	}
	EPI_morea = {
		icon = mission_non-western_cavalry_raid
		position = 3
		required_missions = { EPI_hellas }
		provinces_to_highlight = {
			area = morea_area
			NOT = { country_or_non_sovereign_subject_holds = ROOT }
		}
		trigger = {
			morea_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		effect = {
			give_claims = { area = aegean_archipelago_area }
		}
	}
}

epirote_wars_4 = {
	slot = 4
	generic = no
	ai = yes
	potential = { tag = EPI }
	has_country_shield = yes

	EPI_macedonia = {
		icon = mission_non-western_cavalry_raid
		position = 2
		required_missions = { EPI_force_limit }
		provinces_to_highlight = {
			area = macedonia_area
			NOT = { country_or_non_sovereign_subject_holds = ROOT }
		}
		trigger = {
			owns = 148
			macedonia_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		effect = {
			give_claims = { area = vardarska_area }
			give_claims = { area = thrace_area }
		}
	}
	EPI_thracia = {
		icon = mission_non-western_cavalry_raid
		position = 3
		required_missions = { EPI_macedonia }
		provinces_to_highlight = {
			area = thrace_area
			NOT = { country_or_non_sovereign_subject_holds = ROOT }
		}
		trigger = {
			owns = 151
			thrace_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		effect = {
			add_prestige = 25
			give_claims = { region = balkan_region }
			give_claims = { area = aydin_area }
			give_claims = { area = hudavendigar_area }
			give_claims = { area = kastamonu_area }
			add_permanent_claim = 330
		}
	}
	EPI_nicaea = {
		icon = mission_non-western_cavalry_raid
		position = 4
		required_missions = { EPI_thracia }
		provinces_to_highlight = {
			OR = {
				area = aydin_area
				area = hudavendigar_area
			}
			NOT = { country_or_non_sovereign_subject_holds = ROOT }
		}
		trigger = {
			owns = 4942
			aydin_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
			hudavendigar_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		effect = {
			add_prestige = 15
			give_claims = { area = ankara_area }
			give_claims = { area = germiyan_area }
			give_claims = { area = cukurova_area }
		}
	}
}

epirote_wars_5 = {
	slot = 5
	generic = no
	ai = yes
	potential = { tag = EPI }
	has_country_shield = yes

	EPI_trapezous = {
		icon = mission_non-western_cavalry_raid
		position = 4
		required_missions = { EPI_thracia }
		provinces_to_highlight = {
			OR = {
				area = kastamonu_area
				province_id = 330
			}
			NOT = { country_or_non_sovereign_subject_holds = ROOT }
		}
		trigger = {
			owns = 330
			kastamonu_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		effect = {
			add_prestige = 25
			give_claims = { area = rum_area }
			give_claims = { area = karaman_area }
			give_claims = { area = erzurum_area }
		}
	}
	EPI_anatolia = {
		icon = the_latin_empire
		position = 5
		required_missions = { EPI_nicaea EPI_trapezous }
		provinces_to_highlight = {
			OR = {
				area = germiyan_area
				area = rum_area
				area = karaman_area
				area = ankara_area
				area = erzurum_area
				area = cukurova_area
			}
			NOT = { country_or_non_sovereign_subject_holds = ROOT }
		}
		trigger = {
			num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				OR = {
					area = germiyan_area
					area = rum_area
					area = karaman_area
					area = ankara_area
					area = erzurum_area
					area = cukurova_area
				}
				value = 20
			}
		}
		effect = {
			add_prestige = 10
			give_claims = { region = anatolia_region }
		}
	}
}
